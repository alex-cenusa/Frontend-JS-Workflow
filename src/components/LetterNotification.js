const LETTER_NOTIFICATION = () => {
  const template = `
    <div class="notification-container" id="notification-container">
    <p>You have already entered this letter</p>
  </div>
    
    `;

  return template;
}
 

export default LETTER_NOTIFICATION;

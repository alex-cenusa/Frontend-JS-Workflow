const HEADER = () => {
  const template = `
		<header>
        <h1>My parcel app</h1>
        <p>this is a boilerplate for a simple js workflow</p>
		</header>

    `;

  return template;
};

export default HEADER;
